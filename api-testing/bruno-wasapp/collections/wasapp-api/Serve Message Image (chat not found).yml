info:
  name: Serve Message Image (chat not found)
  type: http
  seq: 69

http:
  method: GET
  url: "{{baseUrl}}/chats/00000000-0000-0000-0000-000000000000/images/{{imageMsgId}}"
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(404);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
