info:
  name: Set Group Avatar (non-member)
  type: http
  seq: 38

http:
  method: PUT
  url: "{{baseUrl}}/chats/{{groupChatId}}/avatar"
  body:
    type: multipart-form
    data:
      - name: binaryImage
        type: file
        value:
          - /Users/rima/Documents/Dev/Uni/WASA/wasapp/api-testing/bruno-wasapp/assets/test-group-image.jpg
        contentType: image/jpeg
  auth:
    type: bearer
    token: "{{tokenD}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(403);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
