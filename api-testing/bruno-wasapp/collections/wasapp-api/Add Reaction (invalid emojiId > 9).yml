info:
  name: Add Reaction (invalid emojiId > 9)
  type: http
  seq: 74

http:
  method: POST
  url: "{{baseUrl}}/chats/{{directChatId}}/messages/{{msgId2}}/reactions"
  body:
    type: json
    data: |-
      {
        "emojiId": 10
      }
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(400);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
