info:
  name: User Search
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/users?username="
  params:
    - name: username
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
