info:
  name: Request User Avatar
  type: http
  seq: 46

http:
  method: GET
  url: "{{baseUrl}}/users/{{userId}}/avatar"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
