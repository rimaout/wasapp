info:
  name: Get Direct Chat Avatar
  type: http
  seq: 41

http:
  method: GET
  url: "{{baseUrl}}/chats/{{directChatId}}/avatar"
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(200);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
