info:
  name: Leave Group (as D)
  type: http
  seq: 26

http:
  method: DELETE
  url: "{{baseUrl}}/chats/{{groupChatId}}/members/me"
  auth:
    type: bearer
    token: "{{tokenD}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(204);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
