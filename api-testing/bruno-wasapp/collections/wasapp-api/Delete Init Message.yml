info:
  name: Delete Init Message
  type: http
  seq: 65

http:
  method: DELETE
  url: "{{baseUrl}}/chats/{{directChatId}}/messages/{{initMsgId}}"
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(400);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
