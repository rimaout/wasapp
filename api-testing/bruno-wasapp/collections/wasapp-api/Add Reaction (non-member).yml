info:
  name: Add Reaction (non-member)
  type: http
  seq: 75

http:
  method: POST
  url: "{{baseUrl}}/chats/{{directChatId}}/messages/{{msgId2}}/reactions"
  body:
    type: json
    data: |-
      {
        "emojiId": 0
      }
  auth:
    type: bearer
    token: "{{tokenC}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(403);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
