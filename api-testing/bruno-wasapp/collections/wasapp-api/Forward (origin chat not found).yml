info:
  name: Forward (origin chat not found)
  type: http
  seq: 91

http:
  method: POST
  url: "{{baseUrl}}/chats/00000000-0000-0000-0000-000000000000/messages/{{msgId2}}/forwards"
  body:
    type: json
    data: |-
      {
        "forwardTo": "{{groupChatId}}"
      }
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(404);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
