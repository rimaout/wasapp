info:
  name: Serve Forwarded Image (visibility)
  type: http
  seq: 84

http:
  method: GET
  url: "{{baseUrl}}/chats/{{groupChatId}}/images/{{imageMsgId}}"
  auth:
    type: bearer
    token: "{{tokenB}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(200);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
