info:
  name: Get Direct Chat Members (as C not member)
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/chats/{{directChatId}}/members"
  auth:
    type: bearer
    token: "{{tokenC}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(403);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
