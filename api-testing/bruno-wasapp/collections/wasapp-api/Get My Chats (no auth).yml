info:
  name: Get My Chats (no auth)
  type: http
  seq: 80

http:
  method: GET
  url: "{{baseUrl}}/chats"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(401);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
