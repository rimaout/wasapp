info:
  name: Add Member to Group (already in)
  type: http
  seq: 23

http:
  method: POST
  url: "{{baseUrl}}/chats/{{groupChatId}}/members"
  body:
    type: json
    data: |-
      {
        "userId": "{{userIdD}}"
      }
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(409);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
