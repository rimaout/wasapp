info:
  name: Add Member to Group (add D)
  type: http
  seq: 22

http:
  method: POST
  url: "{{baseUrl}}/chats/{{groupChatId}}/members"
  body:
    type: json
    data: |-
      {
        "userId": "{{userIdD}}"
      }
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(201);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
