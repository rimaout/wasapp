info:
  name: Set Group Name (bad name)
  type: http
  seq: 32

http:
  method: PATCH
  url: "{{baseUrl}}/chats/{{groupChatId}}/name"
  body:
    type: json
    data: |-
      {
        "groupName": "!!"
      }
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(400);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
