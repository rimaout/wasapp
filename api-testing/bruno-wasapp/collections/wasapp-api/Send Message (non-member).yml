info:
  name: Send Message (non-member)
  type: http
  seq: 55

http:
  method: POST
  url: "{{baseUrl}}/chats/{{directChatId}}/messages"
  body:
    type: multipart-form
    data:
      - name: text
        type: text
        value: Outsider message
  auth:
    type: bearer
    token: "{{tokenC}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(403);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
