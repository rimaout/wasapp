info:
  name: Create Direct Chat (duplicate)
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/user/{{userIdB}}/chats"
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(409);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
