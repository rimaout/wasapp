info:
  name: Serve Message Image
  type: http
  seq: 66

http:
  method: GET
  url: "{{baseUrl}}/chats/{{directChatId}}/images/{{imageMsgId}}"
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(200);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
