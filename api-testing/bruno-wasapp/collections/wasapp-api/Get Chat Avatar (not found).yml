info:
  name: Get Chat Avatar (not found)
  type: http
  seq: 43

http:
  method: GET
  url: "{{baseUrl}}/chats/00000000-0000-0000-0000-000000000000/avatar"
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(404);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
