info:
  name: Create Group Chat (duplicate member)
  type: http
  seq: 17

http:
  method: POST
  url: "{{baseUrl}}/chats"
  body:
    type: json
    data: |-
      {
        "groupName": "Duplicate Test",
        "membersList": ["{{userIdB}}", "{{userIdB}}"]
      }
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(409);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
