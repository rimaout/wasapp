info:
  name: Set Group Name (non-member)
  type: http
  seq: 34

http:
  method: PATCH
  url: "{{baseUrl}}/chats/{{groupChatId}}/name"
  body:
    type: json
    data: |-
      {
        "groupName": "Hijacked"
      }
  auth:
    type: bearer
    token: "{{tokenD}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(403);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
