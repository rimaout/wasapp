info:
  name: Get My Chats (A)
  type: http
  seq: 78

http:
  method: GET
  url: "{{baseUrl}}/chats"
  auth:
    type: bearer
    token: "{{tokenA}}"

runtime:
  scripts:
    - type: tests
      code: |-
        expect(res.getStatus()).to.equal(200);
        expect(res.getBody().chatsPreviewList.length).to.be.greaterThanOrEqual(2);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
