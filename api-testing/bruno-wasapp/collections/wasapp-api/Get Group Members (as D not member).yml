info:
  name: Get Group Members (as D not member)
  type: http
  seq: 21

http:
  method: GET
  url: "{{baseUrl}}/chats/{{groupChatId}}/members"
  auth:
    type: bearer
    token: "{{tokenD}}"

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(403);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
