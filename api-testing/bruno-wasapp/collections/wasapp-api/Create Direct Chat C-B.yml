info:
  name: Create Direct Chat C→B
  type: http
  seq: 81

http:
  method: POST
  url: "{{baseUrl}}/user/{{userIdB}}/chats"
  auth:
    type: bearer
    token: "{{tokenC}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        bru.setVar("bcChatId", data.id);
    - type: tests
      code: expect(res.getStatus()).to.equal(201);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
